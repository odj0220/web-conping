/**
 * @module ProductModule
 * @param {number} a - 첫 번째 숫자
 * @param {number} b - 두 번째 숫자
 * @returns {number} 첫 번째 숫자와 두 번째 숫자를 합한 값
 * @function sum
 * @description 덧셈 함수.
 * */
const sum = (a: number, b:number): number => {
    return a + b;
}